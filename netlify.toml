[build]
# Directory containing your static site (if the index.html is at the root, just use "/")
publish = "/"
# No build command is needed for a static site with just an index.html
command = ""

[[redirects]]
from = "/*"
to = "/index.html"
status = 200

# Explicitly specifying no plugins
[plugins]